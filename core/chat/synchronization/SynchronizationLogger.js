/**
 * Privacy-safe desktop synchronization logger.
 */
class SynchronizationLogger {
  /** @param {object} _event Event. */
  debug(_event) {}

  /** @param {object} _event Event. */
  info(_event) {}

  /** @param {object} _event Event. */
  warn(_event) {}

  /** @param {object} _event Event. */
  error(_event) {}
}

module.exports = SynchronizationLogger;
