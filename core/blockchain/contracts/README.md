# Blockchain Contracts

Phase 1 intentionally does not include contract bindings. Future phases should add contract adapters here and expose them only through `BlockchainService.getContractAccess()`.
