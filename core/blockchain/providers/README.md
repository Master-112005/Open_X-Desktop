# Blockchain Providers

Provider-specific adapters belong here when OpenX needs alternatives to the default `ethers` JSON-RPC provider. No production code outside `core/blockchain` should instantiate Avalanche providers directly.
